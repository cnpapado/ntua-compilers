(include_subdirs unqualified)

(ocamllex
  (modules lexer)
)

; (ocamlyacc
;   (modules parser)
; )

(executable
  (public_name grace)
  (name main)
  (libraries core core_unix.command_unix)
)

; (rule
  ; (alias runtest)
;;  (deps ../misc/test-progs/fib.eds)
  ; (action (run grace ../misc/test-progs/fib.eds))
; )